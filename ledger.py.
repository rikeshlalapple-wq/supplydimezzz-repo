import sys

def main_menu():
    while True:
        print("\n" + "="*30)
        print(" UNIFIED NUMISMATIC SPATIAL LEDGER v3.0 ")
        print("="*30)
        print("[1] Audit & Log Paper Currency")
        print("[2] Catalog Coin Error/Variation")
        print("[3] View Analytics")
        print("[4] Exit Engine")
        
        choice = input("\nSelect Mode: ")

        if choice == '1':
            print("Auditing...")
        elif choice == '2':
            print("Cataloging errors...")
        elif choice == '4':
            print("Shutting down ledger pipeline. Securely logged.")
            break
        else:
            print("[ERROR] Invalid processing mode selected.")

if __name__ == "__main__":
    main_menu()
